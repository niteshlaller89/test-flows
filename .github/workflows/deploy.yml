name: Deploy blue

on:
  deployment:

jobs:
  prepare:
    name: ${{ github.event.deployment.environment }} deployment in progress
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v6
        with:
          script: |
            console.log(JSON.stringify(context))
      - name: Deloy For Smoke Test
        run: |
          sleep 60